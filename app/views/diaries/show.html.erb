<h1>labo_diary</h1>
<h3>投稿詳細</h3>
<div class="diary">
  <p><%= @diary.body %></p>
  <p><%= @diary.created_at %></p>
</div>

<%= link_to "投稿一覧に戻る", diaries_path %>

<%= link_to "編集する", edit_diary_path(@diary.id) %>